# dependencies
node_modules/
.venv/
venv/
__pycache__/
*.pyc

# environment / secrets — never commit keys
.env
.env.local
.env.*.local

# builds
.next/
out/
build/
*.tsbuildinfo

# worker runtime cache (raw Yahoo responses; audit trail only, not source)
data/raw/

# misc
.DS_Store
*.log
